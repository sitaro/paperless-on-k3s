---
apiVersion: v1
kind: Service
metadata:
  name: db
spec:
  selector:
    app: paperless-backend
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: broker
spec:
  selector:
    app: paperless-backend
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: webserver
spec:
  selector:
    app: paperless-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000